# Skill Router 设计文档

## 概述

NOTE: This document contains some legacy references (e.g. `~/.claude/commands/`) from the earlier Commands-based layout. The project is transitioning to Skills bundles under `~/.claude/skills/<dir>/SKILL.md`.

一个本地 Web 应用，帮助用户可视化管理 Claude Code 的 skills。

## 核心功能

- 扫描 `~/.claude/commands/` 目录下的所有 skill 文件
- 解析每个 skill 的 frontmatter（name、description 等）
- 以卡片形式展示所有 skills
- 支持启用/禁用（移动到 `~/.claude/skills-disabled/`）
- 支持删除 skill
- 支持从 GitHub URL 安装 skills
- 支持上传本地 .md 文件添加 skills

## 技术栈

- **后端**: Go（编译为单个二进制）
- **前端**: Vue 3 + Vite + Tailwind CSS
- **分发**: 前端打包进 Go 二进制（使用 `go:embed`），单文件分发

## 用户体验

1. 下载一个二进制文件
2. 终端运行 `skill-router`
3. 自动打开浏览器 `http://localhost:9527`
4. 看到所有 skills，进行管理操作

## API 设计

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/skills` | 获取所有 skills（启用+禁用） |
| POST | `/api/skills/:name/disable` | 禁用 skill |
| POST | `/api/skills/:name/enable` | 启用 skill |
| DELETE | `/api/skills/:name` | 删除 skill |
| POST | `/api/skills/upload` | 上传本地 .md 文件 |
| POST | `/api/skills/install` | 从 GitHub URL 安装 |

## 数据模型

```go
type Skill struct {
    Name        string `json:"name"`
    Description string `json:"description"`
    FilePath    string `json:"filePath"`
    Enabled     bool   `json:"enabled"`
}
```

## 目录扫描逻辑

- 扫描 `~/.claude/commands/`（用户级 skills）
- 扫描 `~/.claude/skills-disabled/`（禁用的 skills）
- 解析 `.md` 文件的 frontmatter

## 禁用机制

移动文件到 `~/.claude/skills-disabled/` 目录，启用时移回 `~/.claude/commands/`。

## GitHub 安装逻辑

- 支持输入：`https://github.com/user/repo` 或具体路径
- 解析 URL，通过 GitHub API 获取 `.claude/commands/` 目录内容
- 下载所有 `.md` 文件到本地 `~/.claude/commands/`

## 上传逻辑

- 前端拖拽/选择 `.md` 文件
- 后端保存到 `~/.claude/commands/`
- 文件名冲突时提示用户是否覆盖

## UI 设计

```
┌─────────────────────────────────────────────┐
│  Skill Router                    [+ 添加]   │
├─────────────────────────────────────────────┤
│  [全部] [已启用] [已禁用]    🔍 搜索...      │
├─────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐           │
│  │ skill-name  │  │ skill-name  │           │
│  │ description │  │ description │           │
│  │ ────────────│  │ ────────────│           │
│  │ [禁用] [删除]│  │ [启用] [删除]│           │
│  └─────────────┘  └─────────────┘           │
└─────────────────────────────────────────────┘
```

- 卡片式展示，响应式布局
- 筛选标签切换（全部/启用/禁用）
- 搜索框实时过滤
- 点击「+ 添加」弹出 modal，选择 GitHub URL 或上传文件
- 禁用/启用/删除操作有确认提示

## 项目结构

```
skill-router/
├── main.go                 # 入口，启动服务+打开浏览器
├── go.mod
├── internal/
│   ├── handler/            # HTTP handlers
│   │   └── skill.go
│   ├── service/            # 业务逻辑
│   │   └── skill.go
│   ├── parser/             # frontmatter 解析
│   │   └── markdown.go
│   └── github/             # GitHub API 调用
│       └── client.go
├── web/                    # 前端 Vue 项目
│   ├── src/
│   │   ├── components/
│   │   │   ├── SkillCard.vue
│   │   │   ├── SkillList.vue
│   │   │   └── AddSkillModal.vue
│   │   ├── views/
│   │   │   └── Home.vue
│   │   ├── api/
│   │   │   └── skills.ts
│   │   ├── App.vue
│   │   └── main.ts
│   ├── package.json
│   └── vite.config.ts
├── embed.go                # 嵌入前端静态文件
└── Makefile                # 构建脚本
```

## 构建流程

1. `cd web && npm run build` - 构建前端
2. `go build` - Go 使用 `//go:embed` 嵌入 `web/dist/`
3. 输出单个二进制文件
